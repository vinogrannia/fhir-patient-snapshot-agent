# App

Python application code for the FHIR Patient Snapshot Agent will live here.

Planned modules:

- `fhir_client.py` for FHIR API access
- resource normalisers for clinical context extraction
- summarisation agent entry point
